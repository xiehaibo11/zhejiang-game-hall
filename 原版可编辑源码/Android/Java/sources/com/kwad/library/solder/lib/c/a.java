package com.kwad.library.solder.lib.c;

public final class a implements Comparable<a> {
    public String agy;
    public boolean qB;
    public String version;

    @Override
    private int compareTo(a aVar) {
        return aVar.version.compareTo(this.version);
    }
}
