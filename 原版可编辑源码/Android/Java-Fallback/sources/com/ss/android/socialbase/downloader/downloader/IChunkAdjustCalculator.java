package com.ss.android.socialbase.downloader.downloader;

public interface IChunkAdjustCalculator {
    int calculateChunkCount(int r1, com.ss.android.socialbase.downloader.network.NetworkQuality r2);
}
